import { AppHighlitetodoDirective } from './app-highlitetodo.directive';

describe('AppHighlitetodoDirective', () => {
  it('should create an instance', () => {
    const directive = new AppHighlitetodoDirective();
    expect(directive).toBeTruthy();
  });
});
